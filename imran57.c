


#include <stdio.h>
#include<string.h>
int main()
{
   int x,y,t;
    printf("enter the three values");
   scanf("%d %d",&x,&y);
   if(a!=0)
   {
       t=x;
       x=y;
       y=t;
   }
   printf("%d %d",x,y);
    return 0;
}
