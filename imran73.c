
#include <stdio.h>
#include<conio.h>

int main(void) {
int n,l,r;
scanf("%d%d%d",&n,&l,&r);
if((n>=l)&&(n<=r))
{
	printf("no is in between the range");
}
else
{
		printf("no is not in between the range");
}
	return 0;
}
getch();
}
